/// The visual mode that drives Mosaic's token set.
///
/// `metro` is the default: flat, sharp, no elevation, snappy motion.
/// `modern` softens radius and adds subtle elevation while keeping the
/// strict grid and surface model intact.
enum MosaicMode { metro, modern }
